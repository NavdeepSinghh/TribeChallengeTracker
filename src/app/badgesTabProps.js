export function buildBadgesTabProps({
  badgeCat,
  challengeStats,
  earnedBadges,
  myHistory,
  setBadgeCat,
  userProfile,
}) {
  return {
    badgeCat,
    challengeStats,
    earnedBadges,
    myHistory,
    setBadgeCat,
    userProfile,
  };
}
