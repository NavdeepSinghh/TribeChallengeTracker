export function buildBoardTabProps({
  actCounts,
  allActivities,
  daysActive,
  setShowLog,
  setTab,
  streak,
  totalPts,
  user,
  userProfile,
}) {
  return {
    actCounts,
    allActivities,
    daysActive,
    setShowLog,
    setTab,
    streak,
    totalPts,
    user,
    userProfile,
  };
}
