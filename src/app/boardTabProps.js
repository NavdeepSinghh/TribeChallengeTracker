export function buildBoardTabProps({
  actCounts,
  allActivities,
  daysActive,
  setTab,
  streak,
  totalPts,
  user,
}) {
  return {
    actCounts,
    allActivities,
    daysActive,
    setTab,
    streak,
    totalPts,
    user,
  };
}
