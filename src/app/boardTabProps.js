export function buildBoardTabProps({
  actCounts,
  allActivities,
  daysActive,
  setTab,
  streak,
  totalPts,
  user,
  userProfile,
}) {
  return {
    actCounts,
    allActivities,
    daysActive,
    setTab,
    streak,
    totalPts,
    user,
    userProfile,
  };
}
