export function buildBoardTabProps({
  actCounts,
  allActivities,
  daysActive,
  setTab,
  streak,
  totalPts,
}) {
  return {
    actCounts,
    allActivities,
    daysActive,
    setTab,
    streak,
    totalPts,
  };
}
