export function buildHomeTabProps({
  actCounts,
  challengeStats,
  earnedBadges,
  handleProgressShare,
  handleShareTemplateSelect,
  hasActivePro,
  myChallenges,
  myHistory,
  rankRules,
  savingShareTemplate,
  setSelectedDay,
  setShowLog,
  setShowProfile,
  setTab,
  shareTemplateId,
  streak,
  totalPts,
  userProfile,
}) {
  return {
    actCounts,
    challengeStats,
    earnedBadges,
    handleProgressShare,
    handleShareTemplateSelect,
    hasActivePro,
    myChallenges,
    myHistory,
    rankRules,
    savingShareTemplate,
    setSelectedDay,
    setShowLog,
    setShowProfile,
    setTab,
    shareTemplateId,
    streak,
    totalPts,
    userProfile,
  };
}
