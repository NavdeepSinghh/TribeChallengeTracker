import { renderAppTabContent } from "./renderAppTabContent";

export default function AppTabContent({
  actCounts,
  allActivities,
  badgeCat,
  challengeStats,
  daysActive,
  earnedBadges,
  handleChallengeJoinHandled,
  handleChallengeStatsChanged,
  handleProgressShare,
  handleShareTemplateSelect,
  hasActivePro,
  isAdmin,
  myChallenges,
  myHistory,
  pendingJoinCode,
  pendingReferralUid,
  rankRules,
  savingShareTemplate,
  setBadgeCat,
  setMyHistory,
  setSelectedDay,
  setShowLog,
  setShowProfile,
  setTab,
  setUserProfile,
  shareTemplateId,
  streak,
  tab,
  totalPts,
  triggerBadgeCheck,
  user,
  userProfile,
}) {
  return renderAppTabContent({
    actCounts,
    allActivities,
    badgeCat,
    challengeStats,
    daysActive,
    earnedBadges,
    handleChallengeJoinHandled,
    handleChallengeStatsChanged,
    handleProgressShare,
    handleShareTemplateSelect,
    hasActivePro,
    isAdmin,
    myChallenges,
    myHistory,
    pendingJoinCode,
    pendingReferralUid,
    rankRules,
    savingShareTemplate,
    setBadgeCat,
    setMyHistory,
    setSelectedDay,
    setShowLog,
    setShowProfile,
    setTab,
    setUserProfile,
    shareTemplateId,
    streak,
    tab,
    totalPts,
    triggerBadgeCheck,
    user,
    userProfile,
  });
}
